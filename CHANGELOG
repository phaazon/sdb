Change Log
----------

Version Tag: v0.9.3-120112
    - fixed file existence trouble
    - now use the skp.log logger module

Version Tag: v0.9.2-120112
    - full support of modules dependencies
    - fixed bugs related to exception catching

Version Tag: v0.9.1-112712
    - made compilers detection runtime whereas it used to be compile-time
    - sdb configuration directory is customizable
    - made a further use of sdb configuration file per compiler
    - added the « with » CLI option for compiler use
    - added the « scan » CLI option for modules scan
    - the « root » dir is now automatically added in the « import dir »
    - improved the portability and make the project configuration file OS-independant
    - removed the « auto scan » option, which is quite useless and can be simulated with shell aliases
    - removed output log when trying to compile an already up to date compiled file
    - integration test development is frozen for now; the development will start again for the v1.1
    - updated the samples for v0.9.1
